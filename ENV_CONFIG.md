# 环境配置说明

## 📁 配置文件

配置文件位于项目根目录：`config.json`

```json
{
  "environments": {
    "test": {
      "name": "测试环境",
      "url": "http://test-otc.68chat.co/",
      "description": "Test Environment"
    },
    "uat": {
      "name": "预发布环境",
      "url": "http://uat-otc.68chat.co/",
      "description": "UAT Environment"
    },
    "prod": {
      "name": "生产环境",
      "url": "http://otc.68chat.co/",
      "description": "Production Environment"
    }
  },
  "default_env": "test",
  "devtools_enabled": false
}
```

## 🔧 配置项说明

### environments
定义所有可用的环境，每个环境包含：
- `name`: 环境显示名称（中文）
- `url`: 环境访问地址
- `description`: 环境描述（英文）

### default_env
默认启动的环境，可选值：`test`、`uat`、`prod`

### devtools_enabled
是否自动打开开发者工具
- `true`: 启动时自动打开（开发/调试用）
- `false`: 默认不打开（生产环境推荐）

## 🎯 使用方法

### 1. 切换环境

在应用菜单栏选择：**环境切换** → 选择目标环境

- 测试环境 (test)
- 预发布环境 (uat)
- 生产环境 (prod)

### 2. 打开开发者工具

在应用菜单栏选择：**调试** → **打开开发者工具**

或使用快捷键（如果配置了）

### 3. 修改配置

直接编辑 `config.json` 文件，然后重启应用即可生效

#### 添加新环境

```json
"environments": {
  "test": { ... },
  "uat": { ... },
  "prod": { ... },
  "dev": {
    "name": "开发环境",
    "url": "http://localhost:8080/",
    "description": "Development Environment"
  }
}
```

#### 修改默认环境

```json
"default_env": "prod"
```

#### 启用开发者工具

```json
"devtools_enabled": true
```

## 🔒 安全验证

所有环境的请求都会自动添加安全验证头：
- `X-Client-Signature`: RSA 加密签名
- `X-Timestamp`: 时间戳
- `X-Device-Fingerprint`: 设备指纹

无需手动配置，自动适配所有环境。

## 📝 注意事项

1. 配置文件不存在时，会使用默认配置（test 环境）
2. URL 必须以 `http://` 或 `https://` 开头
3. 环境切换会重新加载页面
4. 生产环境建议关闭 `devtools_enabled`

